[project]
name = "kmb-bus-tui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "josefgst", email = "josefgstoettner@live.at" }
]
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
    "requests>=2.32",
]

[project.scripts]
kmb-bus-tui = "kmb_bus_tui:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
